# HANDOFF-BLOCKERS — OMNIS CLAW 2.0 rebase 2026-08-31

Builder: Tifa. Documented block = success. No self-CLEAR.

## Needs Captain

| Item                      | File:line                           | Question                                                                                                                                                                                                  |
| ------------------------- | ----------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Live `~/.openclaw` backup | `docs/SQLITE-2.0-INHERIT.md`        | Run `openclaw backup create --output ~/Backups/openclaw --verify` **before** pointing a 2.0 binary at the live state dir. This rebase does not do that.                                                   |
| Push / npm / Fly          | —                                   | None until gated.                                                                                                                                                                                         |
| Apache LICENSE            | `LICENSE:1` (MIT)                   | Specified HEAD `bd3d22e970` is still MIT. Apache prep lives on `codex/claw-apache-release-prep-20260809` (`6b9e1b26df`, `0349e8d317`), not on the rebase source HEAD. Land Apache on 2.0 in a later pass? |
| Full-tree branding        | 21480 files match `openclaw`        | JL front door is OMNIS CLAW. The inherited 2.0 tree still says OpenClaw. A full rebrand is not this pass. Want a follow-on sweep?                                                                         |
| CLI display name          | `src/entry.version-fast-path.ts:58` | Banner still `OpenClaw ${VERSION}`. Version is `2026.8.1-omnisclaw.0`. Port `resolveProductDisplayName` from `22d9d1a741`?                                                                                |

## Needs Bulma

| Item                      | File:line                                                                                                                                                                        | Question                                                                                                                                                                                                                 |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| TERMINUS exec path on 2.0 | `src/process/exec.ts` `runExec` (~L40); `src/process/exec-runner.ts` `runCommandWithOutputEncoding` (~L95); `src/agents/bash-tools.exec-runtime.ts` `runExecProcess` (~L706–712) | Confirm all three call `assertTerminusAllow` **before** spawn; `runExecProcess` gates **both** `opts.command` and `opts.execCommand` when they differ; 2.0 `beforeSpawn` exec-approval still runs (compose, not shadow). |
| Can-fail                  | `src/omnisclaw/terminus/action-gate.runtime.test.ts` (gate off); scratch `scripts/_canfail-raw-spawn.mts` ran then **deleted**                                                   | Gate off → `assertTerminusAllow("rm -rf /")` does not throw. Raw `spawnCommand` deleted a **dedicated temp dir** (not `/`). Confirm that is the load-bearing proof you want, or require a different can-fail shape.      |
| Chain                     | CADUCEUS `receipts.mjs` `verifyChain` on a **copy** of `caduceus/receipts/actions.jsonl`                                                                                         | Clean `{ok:true,count:38}`; tamper last `payload_digest` → `{ok:false,reason:sig_mismatch}`. CLAW does not own the ledger.                                                                                               |
| `human_override`          | CADUCEUS `terminus-action.mjs:826` via `authorizeActionCli`                                                                                                                      | HOLD lifts; REFUSE does not. CLAW `assertTerminusAllow` has no override knob — override is a CADUCEUS action field, not an exec-path bypass.                                                                             |

## Other (logged, not faked)

| Item                              | Fact                                                                                                                                                                                                                                                                                                                         |
| --------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Provider-gate intercept           | `d05f1f91b3` persisted `src/omnisclaw/terminus/provider-gate-live-artifact.json` and a verify-plan that calls `provider-gate.test.ts`. That test file and `provider-gate.mjs` are **not** on `bd3d22e970`. Untracked copies on the dirty main checkout were not promoted. Residency-block (`api.moonshot.cn`) not re-proved. |
| `node-llama-cpp`                  | 2.0 has no dependency. Inherit managed `llama-server`.                                                                                                                                                                                                                                                                       |
| Node engines                      | Inherit 2.0: `>=22.22.3 <23 \|\| >=24.15.0 <25 \|\| >=25.9.0`.                                                                                                                                                                                                                                                               |
| Full OpenClaw suite               | Proven: `pnpm test:unit:fast` → 15387 passed / 6 skipped. **Not run:** `pnpm test` / e2e / docker.                                                                                                                                                                                                                           |
| Pinning / onboarding / Control UI | No JL patches on specified HEAD. Inherit. Not re-proved.                                                                                                                                                                                                                                                                     |
| Dirty main checkout               | `/Users/sokpyeon/projects/omnisclaw-openclaw` @ `toph/terminus-action-gate` has uncommitted workflow deletes + untracked terminus files. **Do not commit there.** This work is the worktree only.                                                                                                                            |
